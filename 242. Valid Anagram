 def isAnagram(self, s: str, t: str) -> bool:
        if(sorted(list(s))==sorted(list(t))):
            return True
        else:
            return False
            
            
Example 1:
Input: s = "anagram", t = "nagaram"
Output: true

Example 2:
Input: s = "rat", t = "car"
Output: false
